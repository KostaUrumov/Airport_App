@model ChangePasswordModel
@{
    ViewData["Title"] = "Change Password";
}

<div class="row">
    <div class="col-md-4">
        <form id="registerForm" asp-route-returnUrl="" method="post">
            <h3 class="text-center">@ViewBag.title</h3>
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-floating">
                <input asp-for="@Model.NewPassword" class="form-control" autocomplete="model" aria-required="true" />
                <label asp-for="@Model.NewPassword"></label>
                <span asp-validation-for="@Model.NewPassword" class="text-danger"></span>
            </div>


            <p></p>
            <p></p>

            <div class="form-floating">
                <input asp-for="@Model.RepeatNewPassword" class="form-control" autocomplete="model" aria-required="true" />
                <label asp-for="@Model.RepeatNewPassword"></label>
                <span asp-validation-for="@Model.RepeatNewPassword" class="text-danger"></span>
            </div>

            <p></p>
            <p></p>

            <button id="registerSubmit" type="submit" class="w-100 btn btn-lg btn-primary">Confirm Change</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}