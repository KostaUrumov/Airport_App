@model List<DisplayAirportModel>
@{
    ViewData["Title"] = "All Airports";
}

<div class="text-center">
    <h3>Airports</h3>
</div>
@if (Model.Count == 0)
{
    <div class="text-center">
        <p></p>
        <p></p>
        <p></p>
        <h5>No manufacturers in our data</h5>
        <a class="" asp-area="" asp-controller="Airport" asp-action="AddNewAirport">Add new airport</a>
        <p></p>
        <p></p>

    </div>
}
else
{

    <table class="table">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Country</th>
                <th scope="col">City</th>
                <th scope="col">Name</th>
                <th scope="col">Code</th>
                <th scope="col">Edit</th>
            </tr>
        </thead>

        @for (int i = 0; i < Model.Count; i++)
        {
            int number = @i + 1;
            <tbody>
                <tr>
                    <td>@number</td>
                    <td>@Model[i].Country</td>
                    <td>@Model[i].City</td>
                    <td>@Model[i].Name</td>
                    <td>@Model[i].Code</td>
                    <td>
                        <a asp-controller="Airport" asp-action="Edit" asp-route-id="@Model[i].Id" class="btn btn-warning">Edit</a>
                    </td>
                </tr>
            </tbody>
        }
    </table>
}










